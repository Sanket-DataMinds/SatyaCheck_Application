<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.SatyaCheck" parent="android:Theme.Material.Light.NoActionBar" />
    
    <!-- Splash Screen Theme -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_shield_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.SatyaCheck</item>
    </style>
</resources>

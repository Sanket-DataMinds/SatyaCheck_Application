<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext traffic for localhost connections (emulator) and your development machine -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">192.168.1.101</domain>
    </domain-config>
    
    <!-- Debug configuration for development - remove in production -->
    <debug-overrides>
        <trust-anchors>
            <!-- Trust user added certificates (for debugging with tools like Charles Proxy) -->
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
    
    <!-- Default configuration -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>